diff -ruN original-2013/src/mtu/construction/dialog/SpaceViolationDialog.java src/mtu/construction/dialog/SpaceViolationDialog.java
--- original-2013/src/mtu/construction/dialog/SpaceViolationDialog.java	2010-01-22 11:30:24
+++ src/mtu/construction/dialog/SpaceViolationDialog.java	2026-08-26 16:27:17
@@ -72,14 +72,15 @@
 
 
 
-		addComponent(new JLabel("Out of space. Selet materials from this dilivery to send back"));
-		addComponent(new JLabel("at "+Math.round(overstockPenalty*100)+"% of the origional value"));
+		addComponent(new JLabel("Out of space. Select materials from this delivery to send back"));
+		addComponent(new JLabel("at "+Math.round(overstockPenalty*100)+"% of the original value."));
+		addComponent(new JLabel("Total space available: " + Math.round(100*spaceAllowed)/100.0));
 
 		JPanel headerpanel = new JPanel(new GridLayout(1, 4));
-		headerpanel.add(new JLabel("Material Name"));
-		headerpanel.add(new JLabel("Size"));
-		headerpanel.add(new JLabel("Amount"));
-		headerpanel.add(new JLabel("Space Occupied"));
+		headerpanel.add(new JLabel("Activity"));
+		headerpanel.add(new JLabel(""));
+		headerpanel.add(new JLabel("Enter your space"));
+		headerpanel.add(new JLabel("Space occupied"));
 
 		addComponent(headerpanel);
 
@@ -110,7 +111,7 @@
 		JPanel footerpanel = new JPanel(new GridLayout(1, 4));
 		footerpanel.add(new JLabel());
 		footerpanel.add(new JLabel());
-		footerpanel.add(new JLabel("Total Area:"));
+		footerpanel.add(new JLabel("Total space required:"));
 		footerpanel.add(totallabel);
 		addComponent(footerpanel);
 
@@ -118,7 +119,7 @@
 		JPanel differpanel = new JPanel(new GridLayout(1, 4));
 		differpanel.add(new JLabel());
 		differpanel.add(new JLabel());
-		differpanel.add(new JLabel("To Remove:"));
+		differpanel.add(new JLabel("Space still to remove:"));
 		differpanel.add(diff);
 		addComponent(differpanel);
 
@@ -254,7 +255,7 @@
 
 		add(new JLabel("" + t.getSize()));
 
-		spinner = new JSpinner(new SpinnerNumberModel(quant, 0, 10000, 1));
+		spinner = new JSpinner(new SpinnerNumberModel(quant, 0, Math.max(10000, quant), 1)); // restoration patch: scenario quantities may exceed the old cap
 		spinner.addChangeListener(this);
 		add(spinner);
 
@@ -322,7 +323,7 @@
 		//}
 		double quant = p.getOrder();
 		System.out.println("Quant: "+quant);
-		spinner = new JSpinner(new SpinnerNumberModel(quant, 0, 10000, 1));
+		spinner = new JSpinner(new SpinnerNumberModel(quant, 0, Math.max(10000, quant), 1)); // restoration patch: scenario quantities may exceed the old cap
 		spinner.addChangeListener(this);
 		
 		spacelabel = new JLabel("Space");
diff -ruN original-2013/src/mtu/construction/gui/old/CenterInfoPanel.java src/mtu/construction/gui/old/CenterInfoPanel.java
--- original-2013/src/mtu/construction/gui/old/CenterInfoPanel.java	2009-09-03 13:36:16
+++ src/mtu/construction/gui/old/CenterInfoPanel.java	2026-08-26 15:20:20
@@ -90,17 +90,19 @@
 		G_Variable var;
 		
 		//Weather Picture
+		// restoration patch: scenarios only instantiate variables their rules
+		// reference, so either lookup may be null — default to fair weather / no strike.
 		var = sim.getGlobalVariable("Weather");
-		if(var.getStringState().equals("Rainy"))
+		if(var != null && var.getStringState().equals("Rainy"))
 			weather.setPicture(1);
-		else if(var.getStringState().equals("Snowy"))
+		else if(var != null && var.getStringState().equals("Snowy"))
 			weather.setPicture(2);
 		else
 			weather.setPicture(0);
-		
+
 		//Work Picture
 		var = sim.getGlobalVariable("Global Labor Strike");
-		if(var.getStringState().equals("True"))
+		if(var != null && var.getStringState().equals("True"))
 			worker.setPicture(1);
 		else
 			worker.setPicture(0);
diff -ruN original-2013/src/mtu/construction/gui/old/ConfigPanel.java src/mtu/construction/gui/old/ConfigPanel.java
--- original-2013/src/mtu/construction/gui/old/ConfigPanel.java	2010-01-22 16:06:48
+++ src/mtu/construction/gui/old/ConfigPanel.java	2026-08-26 15:02:12
@@ -103,11 +103,11 @@
 		JLabel userLbl = new JLabel("username:");
 		JLabel passLbl = new JLabel("password:");
 		
-		hostField = new JTextField("construction.eecn.mtu.edu");
-		port = new JTextField("5432");
+		hostField = new JTextField("localhost");
+		port = new JTextField("5433");
 		dbName = new JTextField("vcdb");
-		projectNo = new JTextField("18");
-		username = new JTextField("construction");
+		projectNo = new JTextField("523");
+		username = new JTextField("postgres");
 		password = new JPasswordField();
 		
 		dLeft.add(hostLbl);
diff -ruN original-2013/src/mtu/construction/gui/old/ResourcePanel.java src/mtu/construction/gui/old/ResourcePanel.java
--- original-2013/src/mtu/construction/gui/old/ResourcePanel.java	2009-09-03 13:36:16
+++ src/mtu/construction/gui/old/ResourcePanel.java	2026-08-26 16:07:41
@@ -738,7 +738,10 @@
 		else
 		{
 			base = alloc.getRequested((G_Material)rt);
-			quantity = new JSpinner(new SpinnerNumberModel(base, 0, 1999, 1));
+			// restoration patch: scenario quantities exceed the old 1999 cap (e.g. 6000
+			// cutting members in project 523); a SpinnerNumberModel whose value is out of
+			// bounds throws and kills the panel rebuild mid-turn.
+			quantity = new JSpinner(new SpinnerNumberModel(base, Math.min(0, base), Math.max(1999, base), 1));
 		}
 
 		quantity.addChangeListener(this);
@@ -865,7 +868,7 @@
 		totalOrder = a.getTotalOrdered();
 		double val = 100.0/duration;
 		//System.out.println("Value is: "+val+" = (" +totalOrder+" + "+100.0/duration+")");
-		orderOverall = new JSpinner(new SpinnerNumberModel( ( val > 100-totalOrder ? 100-totalOrder : val ) , 0, 100-totalOrder, 1));
+		orderOverall = new JSpinner(new SpinnerNumberModel( Math.max(0, ( val > 100-totalOrder ? 100-totalOrder : val )) , 0, Math.max(0, 100-totalOrder), 1)); // restoration patch: keep bounds valid when over-ordered
 		val = (100 - totalOrder)*duration;
 		orderDaily = new JSpinner(new SpinnerNumberModel((100 > val ? val : 100), 0, val, 1));
 		allocateLabel = new JLabel();
diff -ruN original-2013/src/mtu/construction/icdma/Simulator.java src/mtu/construction/icdma/Simulator.java
--- original-2013/src/mtu/construction/icdma/Simulator.java	2026-08-26 16:27:26
+++ src/mtu/construction/icdma/Simulator.java	2026-08-26 15:02:02
@@ -145,7 +145,7 @@
 	}
 	
 	private boolean connect(){
-		return connect("construction.eecn.mtu.edu", 5432, "vcdb", 7, "construction", "********");
+		return connect("localhost", 5433, "vcdb", 7, "postgres", "");
 	}
 	
 	/**
diff -ruN original-2013/src/mtu/construction/project/TONAE.java src/mtu/construction/project/TONAE.java
--- original-2013/src/mtu/construction/project/TONAE.java	2010-01-22 11:30:24
+++ src/mtu/construction/project/TONAE.java	2026-08-26 15:06:46
@@ -1418,9 +1418,12 @@
 			if(alloc.getActivity() != null)
 			{
 				//Each of these variables are local variables
-				DiscreteV lab_avail = (DiscreteV)currentState.environment.getVariable(alloc.getActivity(), "Labor Availible");
+				DiscreteV lab_avail = (DiscreteV)currentState.environment.getVariable(alloc.getActivity(), "Labor Available");
 				DiscreteV lab_low = (DiscreteV)currentState.environment.getVariable(alloc.getActivity(), "Low Labor");
 
+				if(lab_avail == null || lab_low == null) // restoration patch: scenario defines no labor-event variables for this activity
+					continue;
+
 				//Check if a global labor strike has occurred
 				if(lab_avail.getState().equals("False"))
 				{
@@ -1669,8 +1672,12 @@
 	 */
 	private void checkMaterial(HashMap<Rule, HashSet<Activity>> rulelist, Vector<ResourceAllocation> request)
 	{
-		DiscreteV mat_avail = (DiscreteV)currentState.environment.getGlobalVariable("Material Availible");
-		if(mat_avail != null && mat_avail.getState().equals("False"))
+		// restoration patch: DB variable is spelled "Material Available"; original code
+		// looked up the misspelled "Material Available" and NPE'd on the second use.
+		DiscreteV mat_avail = (DiscreteV)currentState.environment.getGlobalVariable("Material Available");
+		if(mat_avail == null)
+			return;
+		if(mat_avail.getState().equals("False"))
 			currentState.purchasedmaterial.clear();
 		//Reset the PNode "ordered" variable
 		if(mat_avail.getState().equals("False"))
@@ -2193,6 +2200,8 @@
 			//			if(!querymode)
 			//				System.out.println("Setting driving material to "+(min/10)+" for activity "+a.getLabel());
 			DiscreteV var = (DiscreteV) getEnvironment().getVariable(a, "Driving Material Available");
+			if (var == null) // restoration patch: projects without driving materials never define this variable
+				continue;
 			var.setState(""+(min/10), 1);
 		}
 
diff -ruN original-2013/src/mtu/construction/project/Updater.java src/mtu/construction/project/Updater.java
--- original-2013/src/mtu/construction/project/Updater.java	2010-01-22 15:14:14
+++ src/mtu/construction/project/Updater.java	2026-08-26 15:06:49
@@ -231,6 +231,8 @@
 //			if(!querymode)
 //				System.out.println("Setting driving material to "+(min/10)+" for activity "+a.getLabel());
 			DiscreteV var = (DiscreteV) currentState.environment.getVariable(a, "Driving Material Available");
+			if (var == null) // restoration patch: mirrors TONAE.checkDrivingMaterials guard
+				continue;
 			var.setState(""+(min/10), 1);
 		}
 
@@ -374,9 +376,12 @@
 		{
 			if(alloc.getActivity() != null)
 			{
-				DiscreteV lab_avail = (DiscreteV)currentState.environment.getVariable(alloc.getActivity(), "Labor Availible");
+				DiscreteV lab_avail = (DiscreteV)currentState.environment.getVariable(alloc.getActivity(), "Labor Available");
 				DiscreteV lab_low = (DiscreteV)currentState.environment.getVariable(alloc.getActivity(), "Low Labor");
 
+				if(lab_avail == null || lab_low == null) // restoration patch: mirrors TONAE.checkLabor guard
+					continue;
+
 				//Check if a global labor strike has occurred
 				if(lab_avail.getState().equals("False"))
 				{
@@ -533,7 +538,8 @@
 	 */
 	private void checkMaterial(HashMap<Rule, HashSet<Activity>> rulelist)
 	{
-		DiscreteV mat_avail = (DiscreteV)currentState.environment.getGlobalVariable("Material Availible");
+		// restoration patch: match DB spelling "Material Available" (see TONAE.checkMaterial)
+		DiscreteV mat_avail = (DiscreteV)currentState.environment.getGlobalVariable("Material Available");
 		if(mat_avail != null && mat_avail.getState().equals("False"))
 			currentState.purchasedmaterial.clear();
 	}
